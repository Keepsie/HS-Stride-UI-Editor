<Window x:Class="HS.Stride.UI.Editor.Views.ImportAssetsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Import Assets"
        Width="600"
        Height="500"
        WindowStartupLocation="CenterOwner"
        Background="#1A1A1A"
        ResizeMode="CanResizeWithGrip"
        MinWidth="500"
        MinHeight="400">

    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#2A2A2A"/>
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="BorderBrush" Value="#3A3A3A"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
        <Style TargetType="Button" x:Key="DialogButton">
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="Background" Value="#3A3A3A"/>
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
        <Style TargetType="Button" x:Key="PrimaryDialogButton">
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="Background" Value="#0A84FF"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock x:Name="TitleText"
                       Text="Import Textures"
                       FontSize="20"
                       FontWeight="SemiBold"/>
            <TextBlock x:Name="SubtitleText"
                       Text="Select image files to import as Stride texture assets"
                       FontSize="12"
                       Foreground="#9A9A9A"
                       Margin="0,4,0,0"/>
        </StackPanel>

        <!-- Package Name -->
        <Grid Grid.Row="1" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Package Name:" VerticalAlignment="Center"/>
            <TextBox x:Name="PackageNameTextBox"
                     Grid.Column="1"
                     Text="Textures"
                     ToolTip="Assets will be organized under this folder name"/>
        </Grid>

        <!-- File List -->
        <Grid Grid.Row="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
                <TextBlock Text="Selected Files:" VerticalAlignment="Center"/>
                <TextBlock x:Name="FileCountText"
                           Text="(0 files)"
                           Foreground="#9A9A9A"
                           Margin="8,0,0,0"
                           VerticalAlignment="Center"/>
                <Button Content="Add Files..."
                        Style="{StaticResource DialogButton}"
                        Margin="16,0,0,0"
                        Click="AddFiles_Click"/>
                <Button Content="Clear All"
                        Style="{StaticResource DialogButton}"
                        Margin="8,0,0,0"
                        Click="ClearFiles_Click"/>
            </StackPanel>

            <Border Grid.Row="1"
                    Background="#2A2A2A"
                    BorderBrush="#3A3A3A"
                    BorderThickness="1"
                    CornerRadius="4">
                <ListBox x:Name="FileListBox"
                         Background="Transparent"
                         BorderThickness="0"
                         Foreground="#E0E0E0"
                         ScrollViewer.HorizontalScrollBarVisibility="Auto">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="4,2">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="{Binding FileName}" VerticalAlignment="Center"/>
                                <TextBlock Grid.Column="1"
                                           Text="{Binding FolderPath}"
                                           Foreground="#666666"
                                           FontSize="11"
                                           Margin="16,0,0,0"
                                           VerticalAlignment="Center"/>
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Border>
        </Grid>

        <!-- Status -->
        <Border Grid.Row="3"
                Background="#252525"
                CornerRadius="4"
                Padding="12,8"
                Margin="0,16,0,0">
            <TextBlock x:Name="StatusText"
                       Text="Select files to import"
                       Foreground="#9A9A9A"
                       TextWrapping="Wrap"/>
        </Border>

        <!-- Buttons -->
        <StackPanel Grid.Row="4"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,20,0,0">
            <Button Content="Cancel"
                    Style="{StaticResource DialogButton}"
                    Margin="0,0,10,0"
                    Click="CancelButton_Click"/>
            <Button x:Name="ImportButton"
                    Content="Import"
                    Style="{StaticResource PrimaryDialogButton}"
                    IsEnabled="False"
                    Click="ImportButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
